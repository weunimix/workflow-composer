{
  "name": "workflow-composer",
  "version": "0.1.0",
  "description": "OOP template compiler for pi agents — Runtime 派 BaseAgent → .pi/agents/*.md",
  "type": "module",
  "keywords": [
    "pi-package"
  ],
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build": "tsx src/compiler.ts"
  },
  "dependencies": {
    "yaml": "^2.8.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "tsx": "^4.23.1",
    "typescript": "^5.9.3"
  }
}
