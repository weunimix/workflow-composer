{
  "name": "workflow-composer",
  "version": "0.1.0",
  "description": "OOP template compiler for pi agents — Runtime 派 BaseAgent → agents/*.md",
  "type": "module",
  "keywords": [
    "pi-package"
  ],
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build": "tsx src/compiler.ts"
  },
  "pi-subagents": {
    "agents": [
      "./agents"
    ],
    "prompts": [
      "./prompts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "^0.85.1"
  },
  "dependencies": {
    "yaml": "^2.8.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "tsx": "^4.23.1",
    "typescript": "^5.9.3"
  }
}
