---
description: 解析文本中的工作流关键词，内联展开预置模块后执行。关键词是函数名，预置模块是函数体。
argument-hint: "<包含工作流关键词的任意文本>"
---

## 规则

将用户输入中的工作流关键词展开为预置模块的完整内容，呈现展开结果等待确认，确认后执行。

用户输入: $@

### 步骤

1. 读取 `.pi/workflows/_index.md` 获取关键词→文件映射表
2. 扫描用户输入，检测是否出现 `_index.md` 注册表中的关键词
3. 对每个匹配的关键词：
   - 读取该关键词对应的模块文件（完整读取）
   - 用模块文件的完整 body 内容（frontmatter 以下部分）内联替换关键词在原文中的位置
4. 未匹配的文本原样保留
5. **呈现展开后的完整工作流给用户，等待用户确认**
6. 用户确认后，执行展开后的文本

### 关键约束

- 关键词匹配必须精确——只匹配 `_index.md` 中列出的触发关键词
- 不存在的关键词视为普通文本，不做展开
- 如用户文本中包含多个关键词，各自独立展开，按原顺序拼接
- **步骤 5 不可跳过**——任何含工作流关键词的输入，必须在执行前呈现展开结果
- 展开后的文本中，每个模块的硬约束（mandatory 块）必须被执行
